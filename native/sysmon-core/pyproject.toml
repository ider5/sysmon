[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "sysmon-core"
version = "0.1.0"
description = "Optional native process scanner for SysMon"
requires-python = ">=3.9"

[tool.maturin]
module-name = "sysmon._core"
